class User {
    constructor(name, email, psw, img, perms) {
        let nam = name;
        let emai = email;
        let ps = psw;
        let im = img;
        let perm = perms;
    }
}

module.exports = User;